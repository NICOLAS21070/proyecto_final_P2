<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.*?>
<?import java.lang.String?>
<?import javafx.collections.FXCollections?>

<VBox xmlns="http://javafx.com/javafx/17.0.12"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="org.uniquindio.edu.co.poo.proyecto_final_p2.ViewController.FormularioEnvioController"
      alignment="CENTER"
      spacing="20"
      padding="25"
      style="-fx-background-color: #E3F2FD;">

    <!-- Título -->
    <Label text="🚀 Crear nuevo envío"
           style="-fx-font-size: 22px; -fx-font-weight: bold;"/>

    <!-- Formulario -->
    <GridPane hgap="15" vgap="12">

        <Label text="Origen:" GridPane.rowIndex="0" GridPane.columnIndex="0"/>
        <TextField fx:id="txtOrigen" prefWidth="260"
                   GridPane.rowIndex="0" GridPane.columnIndex="1"/>

        <Label text="Destino:" GridPane.rowIndex="1" GridPane.columnIndex="0"/>
        <TextField fx:id="txtDestino"
                   GridPane.rowIndex="1" GridPane.columnIndex="1"/>

        <Label text="Peso (kg):" GridPane.rowIndex="2" GridPane.columnIndex="0"/>
        <TextField fx:id="txtPeso"
                   GridPane.rowIndex="2" GridPane.columnIndex="1"/>

        <Label text="Volumen (m³):" GridPane.rowIndex="3" GridPane.columnIndex="0"/>
        <TextField fx:id="txtVolumen"
                   GridPane.rowIndex="3" GridPane.columnIndex="1"/>

        <Label text="Dirección:"
               GridPane.rowIndex="4" GridPane.columnIndex="0"/>
        <TextField fx:id="txtDireccion" promptText="Dirección del envío"
                   GridPane.rowIndex="4" GridPane.columnIndex="1"/>

        <Label text="Prioridad:" GridPane.rowIndex="5" GridPane.columnIndex="0"/>
        <ComboBox fx:id="comboPrioridad" prefWidth="160"
                  GridPane.rowIndex="5" GridPane.columnIndex="1">
            <items>
                <FXCollections fx:factory="observableArrayList">
                    <String fx:value="Normal"/>
                    <String fx:value="Alta"/>
                    <String fx:value="Express"/>
                </FXCollections>
            </items>
        </ComboBox>

    </GridPane>

    <!-- Botones -->
    <HBox spacing="12" alignment="CENTER">
        <Button fx:id="btnGuardar"
                text="Guardar Envío"
                style="-fx-background-color: #2196F3; -fx-text-fill: white; -fx-padding: 8 20; -fx-background-radius: 8;"/>

        <Button fx:id="btnCancelar"
                text="Cancelar"
                style="-fx-background-color: #BDBDBD; -fx-padding: 8 20; -fx-background-radius: 8;"/>
    </HBox>

</VBox>
