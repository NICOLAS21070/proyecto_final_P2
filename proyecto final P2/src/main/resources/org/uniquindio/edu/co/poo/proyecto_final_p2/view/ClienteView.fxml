<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns="http://javafx.com/javafx/17.0.12"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="org.uniquindio.edu.co.poo.proyecto_final_p2.ViewController.ClienteController"
            style="-fx-background-color: #F5F7FA;">

    <!-- Barra superior -->
    <top>
        <ToolBar style="-fx-background-color: #1976D2; -fx-padding: 10;">
            <Label text="📦 Panel del Cliente"
                   style="-fx-text-fill: white; -fx-font-size: 20px; -fx-font-weight: bold;" />
            <Pane HBox.hgrow="ALWAYS" />
            <Button fx:id="btnCerrarSesion" text="Cerrar Sesión" onAction="#cerrarSesion"
                    style="-fx-background-radius: 5;
                           -fx-background-color: white;
                           -fx-text-fill: #1976D2;
                           -fx-font-weight: bold;
                           -fx-padding: 6 15;
                           -fx-cursor: hand;" />
        </ToolBar>
    </top>

    <!-- Zona central -->
    <center>
        <VBox alignment="CENTER" spacing="25">
            <padding>
                <Insets top="30" bottom="30" left="40" right="40"/>
            </padding>

            <Label text="Gestión de Envíos"
                   style="-fx-font-size: 18px; -fx-font-weight: bold; -fx-text-fill: #424242;" />

            <HBox alignment="CENTER" spacing="20">
                <Button fx:id="btnCotizar" text="Cotizar y Crear Envío" prefWidth="180"
                        onAction="#cotizarEnvio"
                        style="-fx-background-color: #2196F3;
                               -fx-text-fill: white;
                               -fx-font-weight: bold;
                               -fx-padding: 10 15;
                               -fx-background-radius: 8;
                               -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.2), 5, 0, 0, 2);
                               -fx-cursor: hand;" />

                <Button fx:id="btnModificarEnvio" text="Buscar y/o Modificar Envío" prefWidth="180"
                        onAction="#modificarEnvio"
                        style="-fx-background-color: #42A5F5;
                               -fx-text-fill: white;
                               -fx-font-weight: bold;
                               -fx-padding: 10 15;
                               -fx-background-radius: 8;
                               -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.2), 5, 0, 0, 2);
                               -fx-cursor: hand;" />

                <Button fx:id="btnCancelarEnvio" text="Cancelar Envío" prefWidth="180"
                        onAction="#cancelarEnvio"
                        style="-fx-background-color: #EF5350;
                               -fx-text-fill: white;
                               -fx-font-weight: bold;
                               -fx-padding: 10 15;
                               -fx-background-radius: 8;
                               -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.2), 5, 0, 0, 2);
                               -fx-cursor: hand;" />
            </HBox>

            <Separator />

            <Label text="Seguimiento y Pagos"
                   style="-fx-font-size: 18px; -fx-font-weight: bold; -fx-text-fill: #424242;" />

            <HBox alignment="CENTER" spacing="20">
                <Button fx:id="btnRastrear" text="Rastrear Envío" prefWidth="180"
                        onAction="#rastrearEnvio"
                        style="-fx-background-color: #66BB6A;
                               -fx-text-fill: white;
                               -fx-font-weight: bold;
                               -fx-padding: 10 15;
                               -fx-background-radius: 8;
                               -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.2), 5, 0, 0, 2);
                               -fx-cursor: hand;" />

                <Button fx:id="btnPagos" text="Pagos y Comprobantes" prefWidth="180"
                        onAction="#consultarPagos"
                        style="-fx-background-color: #29B6F6;
                               -fx-text-fill: white;
                               -fx-font-weight: bold;
                               -fx-padding: 10 15;
                               -fx-background-radius: 8;
                               -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.2), 5, 0, 0, 2);
                               -fx-cursor: hand;" />

                <Button fx:id="btnHistorial" text="Historial de Envíos" prefWidth="180"
                        onAction="#verHistorial"
                        style="-fx-background-color: #AB47BC;
                               -fx-text-fill: white;
                               -fx-font-weight: bold;
                               -fx-padding: 10 15;
                               -fx-background-radius: 8;
                               -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.2), 5, 0, 0, 2);
                               -fx-cursor: hand;" />
            </HBox>

        </VBox>
    </center>

</BorderPane>
